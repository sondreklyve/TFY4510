# Master Thesis — Hybrid Stars

- `thesis/`: LaTeX source (build with LaTeX Workshop or `make pdf`)
- `numerics/`: Python code (TOV/EoS later)
- `docs/`: notes, weekly summaries

## Build PDF
```bash
make pdf

